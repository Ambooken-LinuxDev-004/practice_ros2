# Require to import Python modules

"""It designates the directory as python package and enables python's import machinary to recognize and to trat as cohesive collection of python code. 

This file must be included inside the folder(that's having the same name as package!!!)"""

